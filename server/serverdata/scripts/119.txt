//guide chat
#include <string.h>
#include <stdio.h>
int main()
{
	/*int id=getPlayer();
	int pid=getChatPlayer();
	char chat[256];
	char ghandle[256];

	strcpy(chat,getChat());
	
	sprintf(ghandle,"NEWB%s",getName(pid));

	if(!isGlobal(ghandle))*/
		return 0;
	
	
}